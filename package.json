{
  "scripts": {
    "build": "del dist && tsc",
    "start": "ts-node -r dotenv-safe/config src/index.ts",
    "watch": "nodemon --watch src --ext ts --exec npm start"
  },
  "devDependencies": {
    "@types/node": "^14.11.1",
    "del-cli": "^3.0.1",
    "nodemon": "^2.0.4",
    "ts-node": "^9.0.0",
    "tslint": "^6.1.3",
    "tslint-config-airbnb": "^5.11.2",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "dotenv-safe": "^8.2.0"
  }
}
